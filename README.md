# TimeKey-SHA256
Dynamic key encryption library based on SHA-256 and 10-minute time slots.

.
├── sha256.c       # SHA-256 algorithm implementation
├── sha256.h       # SHA-256 header and structures
├── timekey.c      # Time-based key generation & encryption logic
└── timekey.h      # Configuration (Interval, Seed) & interfaces